<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- JDBC Driver -->
        <property name="hibernate.connection.driver_class">
            com.mysql.cj.jdbc.Driver
        </property>

        <!-- Database URL -->
        <property name="hibernate.connection.url">
            jdbc:mysql://localhost:3306/employeeCRUDHibernate?useSSL=false&amp;serverTimezone=UTC
        </property>

        <!-- DB Username -->
        <property name="hibernate.connection.username">root</property>

        <!-- DB Password -->
        <property name="hibernate.connection.password">System@123</property>

        <!-- MySQL Dialect -->
        <property name="hibernate.dialect">
            org.hibernate.dialect.MySQL8Dialect
        </property>

        <!-- Show SQL Query in Console -->
        <property name="hibernate.show_sql">true</property>

        <!-- Format SQL Output -->
        <property name="hibernate.format_sql">true</property>

        <!-- Auto Table Creation -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Entity Mapping -->
        <mapping class="com.entity.Employee"/>

    </session-factory>
</hibernate-configuration>
